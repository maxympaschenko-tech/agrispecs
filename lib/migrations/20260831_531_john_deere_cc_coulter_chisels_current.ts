import type { ResultSetHeader, RowDataPacket } from 'mysql2';
import type { DbMigration } from '@/lib/db-migration-types';

type IdRow=RowDataPacket&{id:number};
type Seed={slug:string;model:string};
const VERSION='united-states-current-2026-08';
const URL='https://www.deere.com/en/tillage/field-cultivators/';
const models:Seed[]=[
 {slug:'cc11-coulter-chisel',model:'CC11'},
 {slug:'cc13-coulter-chisel',model:'CC13'},
 {slug:'cc19-coulter-chisel',model:'CC19'},
 {slug:'cc21-coulter-chisel',model:'CC21'},
 {slug:'cc25-coulter-chisel',model:'CC25'},
];
const defs:Array<[string,string,string,string,string|null,number]>=[
 ['Machine Configuration','configuration.type','Machine configuration','text',null,1],['Machine Configuration','configuration.market_scope','Official market scope','text',null,2],
 ['Tillage System','coulter_chisel.working_depth','Working depth','text',null,10],['Tillage System','coulter_chisel.working_speed','Working speed','text',null,20],['Tillage System','coulter_chisel.residue_management','Residue management','text',null,30],['Tillage System','coulter_chisel.residue_conditions','Residue conditions','text',null,40],['Tillage System','coulter_chisel.depth_control','Depth control','text',null,50],
 ['Tractor Requirements','coulter_chisel.hp_per_ft','Required horsepower per foot','text',null,10],['Precision Technology','coulter_chisel.precision_control','Precision tillage technology','text',null,10],
];
async function id(c:Parameters<DbMigration['apply']>[0],sql:string,p:unknown[]=[]){const[r]=await c.query<IdRow[]>(sql,p);if(!r[0])throw new Error('John Deere CC Series migration dependency missing');return Number(r[0].id);}
async function src(c:Parameters<DbMigration['apply']>[0]){const[r]=await c.query<IdRow[]>(`SELECT id FROM sources WHERE name='John Deere' AND domain='deere.com' ORDER BY id LIMIT 1`);if(r[0])return Number(r[0].id);const[x]=await c.query<ResultSetHeader>(`INSERT INTO sources(name,domain,source_type,authority_level) VALUES('John Deere','deere.com','manufacturer','official')`);return Number(x.insertId);}
async function rec(c:Parameters<DbMigration['apply']>[0],sid:number,m:Seed){const externalId=`john-deere-${m.slug}-us-current-2026-08`;const[r]=await c.query<IdRow[]>(`SELECT id FROM source_records WHERE external_id=? LIMIT 1`,[externalId]);if(r[0])return Number(r[0].id);const[x]=await c.query<ResultSetHeader>(`INSERT INTO source_records(source_id,url,external_id,title,raw_reference) VALUES(?,?,?,?,?)`,[sid,URL,externalId,`John Deere ${m.model} current US coulter chisel comparison specifications`,JSON.stringify({captured:'2026-08-31',market:'United States',equipmentType:'Coulter Chisel',source:URL,model:m.model})]);return Number(x.insertId);}
async function put(c:Parameters<DbMigration['apply']>[0],mid:number,vid:number,did:number,rid:number,v:string){await c.query(`INSERT INTO machine_specs(machine_id,machine_version_id,spec_definition_id,value_text,value_number,unit,source_record_id,confidence) VALUES(?,?,?,?,NULL,NULL,?,'official') ON DUPLICATE KEY UPDATE value_text=VALUES(value_text),value_number=NULL,unit=NULL,source_record_id=VALUES(source_record_id),confidence='official'`,[mid,vid,did,v,rid]);}
export const johnDeereCcCoulterChiselsCurrentMigration:DbMigration={id:'20260831_531_john_deere_cc_coulter_chisels_current',description:'Add current John Deere US CC11, CC13, CC19, CC21 and CC25 coulter chisels',async apply(c){await c.query(`INSERT INTO equipment_types(name,slug) VALUES('Coulter Chisel','coulter-chisel') ON DUPLICATE KEY UPDATE name=VALUES(name)`);await c.query(`INSERT INTO manufacturers(name,slug) VALUES('John Deere','john-deere') ON DUPLICATE KEY UPDATE name=VALUES(name)`);const mf=await id(c,`SELECT id FROM manufacturers WHERE slug='john-deere' LIMIT 1`),et=await id(c,`SELECT id FROM equipment_types WHERE slug='coulter-chisel' LIMIT 1`),sid=await src(c);await c.query(`INSERT INTO machine_series(manufacturer_id,equipment_type_id,name,slug) VALUES(?,?,'CC Series Coulter Chisels','cc-series-coulter-chisels') ON DUPLICATE KEY UPDATE name=VALUES(name)`,[mf,et]);const series=await id(c,`SELECT id FROM machine_series WHERE manufacturer_id=? AND equipment_type_id=? AND slug='cc-series-coulter-chisels' LIMIT 1`,[mf,et]);const ids=new Map<string,number>();for(const d of defs){await c.query(`INSERT INTO spec_definitions(section,spec_key,label,value_type,canonical_unit,display_order) VALUES(?,?,?,?,?,?) ON DUPLICATE KEY UPDATE section=VALUES(section),label=VALUES(label),value_type=VALUES(value_type),canonical_unit=VALUES(canonical_unit),display_order=VALUES(display_order)`,d);ids.set(d[1],await id(c,`SELECT id FROM spec_definitions WHERE spec_key=? LIMIT 1`,[d[1]]));}const def=(k:string)=>{const v=ids.get(k);if(!v)throw new Error(`Missing CC definition ${k}`);return v;};for(const m of models){const rid=await rec(c,sid,m);await c.query(`INSERT INTO machines(manufacturer_id,equipment_type_id,series_id,model_name,slug,market_notes,data_status) VALUES(?,?,?,?,?,'Current John Deere United States CC Series coulter chisel lineup','partial') ON DUPLICATE KEY UPDATE series_id=VALUES(series_id),model_name=VALUES(model_name),market_notes=VALUES(market_notes)`,[mf,et,series,m.model,m.slug]);const mid=await id(c,`SELECT id FROM machines WHERE manufacturer_id=? AND equipment_type_id=? AND slug=? LIMIT 1`,[mf,et,m.slug]);await c.query(`UPDATE machine_versions SET is_current=FALSE WHERE machine_id=? AND slug<>?`,[mid,VERSION]);await c.query(`INSERT INTO machine_versions(machine_id,slug,market_code,market_name,configuration,is_current,source_record_id,notes) VALUES(?,?,'US','United States','Current CC Series comparison specification',TRUE,?,'Current John Deere US tillage comparison data captured 2026-08-31. Model-number-derived standard counts and widths are not inferred without current individual US specification rows.') ON DUPLICATE KEY UPDATE is_current=TRUE,source_record_id=VALUES(source_record_id),notes=VALUES(notes)`,[mid,VERSION,rid]);const vid=await id(c,`SELECT id FROM machine_versions WHERE machine_id=? AND slug=? LIMIT 1`,[mid,VERSION]);await put(c,mid,vid,def('configuration.type'),rid,'Coulter chisel');await put(c,mid,vid,def('configuration.market_scope'),rid,'United States current catalog');await put(c,mid,vid,def('coulter_chisel.working_depth'),rid,'6 - 8 in (15.2 - 20.3 cm)');await put(c,mid,vid,def('coulter_chisel.working_speed'),rid,'6 - 7 mph (9.6 - 11.3 km/h)');await put(c,mid,vid,def('coulter_chisel.residue_management'),rid,'Fracture / Mix');await put(c,mid,vid,def('coulter_chisel.residue_conditions'),rid,'Medium / Heavy');await put(c,mid,vid,def('coulter_chisel.depth_control'),rid,'TruSet / Single-Point');await put(c,mid,vid,def('coulter_chisel.hp_per_ft'),rid,'20 - 25 hp/ft (48.95 - 61.2 kW/m)');await put(c,mid,vid,def('coulter_chisel.precision_control'),rid,'TruSet; CC Series is Autonomy Ready from the factory');}}
};
